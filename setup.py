from setuptools import setup, find_packages

setup(
    name="Automation",
    version="1.0",
    author="Albini",
    author_email="Hej@",
    packages=find_packages()
)

# If something is missing
# Add to the manifest.in file.